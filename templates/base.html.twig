<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}OrgaTask{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>📋</text></svg>">
        
        <!-- CSS de l'application -->
        <link href="{{ asset('css/app.css') }}" rel="stylesheet">
        
        {% block stylesheets %}
        {% endblock %}
    </head>
    <body>
        <!-- Navbar Simple -->
        <nav style="background-color: #0d6efd; padding: 1rem; color: white; display: flex; justify-content: space-between; align-items: center;">
            <!-- Logo -->
            <a href="{{ path('app_home') }}" style="color: white; text-decoration: none; font-weight: bold; font-size: 1.5rem;">
                📋 OrgaTask
            </a>
            
            <!-- Navigation Links -->
            <div style="display: flex; gap: 2rem; align-items: center;">
                <a href="{{ path('app_home') }}" style="color: white; text-decoration: none;">Accueil</a>
                
                {% if app.user %}
                    <a href="{{ path('app_task') }}" style="color: white; text-decoration: none;">Tâches</a>
                    <a href="{{ path('app_task_new') }}" style="color: white; text-decoration: none;">Nouvelle Tâche</a>
                    <a href="{{ path('app_data') }}" style="color: white; text-decoration: none;">data</a>
                    
                    <!-- User Info -->
                    <div style="display: flex; align-items: center; gap: 1rem;">


                        <a href="{{ path('app_auth') }}" style="color: white; text-decoration: none;">Profil</a>
                        <a href="{{ path('app_logout') }}" style="color: #ffcccc; text-decoration: none;">Déconnexion</a>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}" style="color: white; text-decoration: none;">Connexion</a>
                    <a href="{{ path('app_register') }}" style="color: white; text-decoration: none; background-color: rgba(255,255,255,0.2); padding: 0.5rem 1rem; border-radius: 0.25rem;">Inscription</a>
                {% endif %}


                    {% if app.user and app.user.name %}
    <span>Bonjour {{ app.user.name }}</span>
{% endif %}
            
            </div>
        </nav>

        {% block body %}{% endblock %}

        {% block javascripts %}
        {% endblock %}
    </body>
</html>